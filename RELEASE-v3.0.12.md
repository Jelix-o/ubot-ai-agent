# UBot V3.0.12

V3.0.12 将 SVG 校验从穷举白名单改为主动能力边界，不包含数据库迁移、管理 API 或预览 URL 变更。

## 修复

- 合法命名且无主动能力的 SVG 属性默认允许，不再因模型采用新的纯绘图属性而拒绝页面。
- 已知坐标、颜色、路径、变换、透明度和字体属性继续执行严格格式校验。
- 事件属性、`href/src`、命名空间、URL/网络引用、脚本协议、SMIL、`image`、`use` 和 `foreignObject` 继续拒绝。

## 发布门槛

- 全量测试、双平台打包和 SHA-256 校验必须通过。
- 生产虚拟群以 GPT 主模型运行原始鹈鹕骑自行车请求，验证发布、HTTPS 200、过期 404、取消通知和无残留。
