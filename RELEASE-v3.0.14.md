# UBot V3.0.14

V3.0.14 修复复杂 SVG/HTML 被手写属性规则误判的问题。网页预览继续在独立域名、无同源 iframe 沙箱和离线 CSP 中运行，不包含数据库迁移、管理 API 或预览 URL 变更。

## 修复

- 网页生成内容仅校验 JSON、大小和完整 HTML 文档外壳，不再维护标签、属性或 SVG 数值白名单。
- 标准 HTML、SVG、CSS、Canvas、SMIL、事件属性和内联 JavaScript 原样发布，复杂动画不再因合法语法差异失败。
- 外部资源与联网代码不再导致发布失败，但仍会被预览响应的 CSP 在浏览器运行时阻止。
- 失败日志增加安全化错误码和生成/修复阶段，不记录用户要求、模型输出或页面正文。

## 发布门槛

- 全量测试、构建、双平台打包、差异检查和 SHA-256 校验必须通过。
- 生产虚拟群使用“SVG 绘制鹈鹕骑自行车 2D 动画”验收发布、可见动画、HTTPS 200、过期 404、取消通知和无残留。
