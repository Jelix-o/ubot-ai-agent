# UBot V1.0.1 Local Completion Audit

Status: local implementation and verification complete. GitHub upload, tag push, GitHub Release upload, and production deployment are now authorized for the V1.0.1 release flow.

## Requirement Evidence

| Requirement | Local evidence | Status |
| --- | --- | --- |
| Ordinary QQ users can enter the admin console with only a QQ account. | `src/admin-http-server.ts` implements viewer login through `buildViewerSession`; `admin/src/views/LoginView.vue` exposes ordinary user mode; `src/admin-http-server.test.ts` covers successful viewer login. | Verified locally |
| Ordinary QQ users can view the group data available to group administrators. | Viewer and group-admin parity is covered in `src/admin-http-server.test.ts` and `scripts/visual-admin-smoke.mjs`; visual screenshots include viewer overview, groups, members, memories, candidates, profiles, knowledge, tasks, audit, and health pages. | Verified locally |
| Ordinary QQ users cannot modify any system setting or content. | Global non-safe `/api/*` guard returns `readonly_session` for viewer sessions; direct viewer mutation tests cover group config, identity, profile records, memories, candidates, knowledge, reminders, DELETE, and PATCH. | Verified locally |
| Viewer access tracks current NapCat group membership. | `isCurrentNapcatGroupMember` is checked for viewer visibility, `canAccessGroup`, and `/api/session` `allowedGroupIds`; tests remove a QQ from NapCat members, verify access is revoked, then add the same QQ to another enabled group and verify the existing viewer session can see the new group without re-login. | Verified locally |
| `defaultVoiceReplyEnabled` is a child switch of `voiceReplyEnabled`. | `admin/src/views/GroupsView.vue` watchers disable default voice when voice is off; `src/services/group-config-service.ts` normalizes and updates the same invariant; tests cover normalization and updates. | Verified locally |
| Closing voice reply also closes default voice reply. | Frontend watcher and backend update logic force `defaultVoiceReplyEnabled=false` when `voiceReplyEnabled=false`; tests assert this behavior. | Verified locally |
| Default voice cannot stay enabled while voice reply is disabled. | Frontend watcher rejects that state and backend normalization/update coerces it to false. | Verified locally |
| Local full-system screenshots are generated. | `release/admin-ui-smoke/` contains 50 PNG files and `_contact-sheet.png`; the local verifier also checks screenshot size, byte length, and sampled pixel diversity. | Verified locally |
| V1.0.1 package is generated locally. | `release/ubot-1.0.1-win.zip` exists and contains dist, admin assets, docs, scripts, skills, and safe example config. | Verified locally |
| Release metadata is aligned to V1.0.1. | `package.json`, `README.md`, `COMMANDS.md`, `RELEASE-v1.0.1.md`, `scripts/package-win.ps1`, and `scripts/publish-github-release.ps1` are covered by static tests. | Verified locally |
| Local acceptance can be rerun unattended. | `scripts/verify-v1.0.1-local.ps1 -WithScreenshots` runs `npm test`, visual screenshots, screenshot pixel smoke, packaging, GitHub Release dry-run, package content checks, forbidden key checks, and `git diff --check`. | Verified locally |
| Sensitive runtime config is not packaged. | Zip checks show no real `.env`, `config/groups.json`, `system-settings.json`, or runtime logs; the archive includes only `config/groups.example.json` and empty runtime scaffolding. | Verified locally |
| User-provided key is not present in diff or package. | Local scans did not find the pasted key in Git diff or `release/ubot-1.0.1-win.zip`. | Verified locally |
| Upload, GitHub Release, tag push, and production deployment. | User restored GitHub upload and deployment scope after local verification. These are handled by the release/deploy flow after this local acceptance audit. | Release scope |

## Latest Local Verification

- `npm test`: 359/359 passed.
- `npm run package:win`: passed and regenerated `release/ubot-1.0.1-win.zip`.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/publish-github-release.ps1 -DryRun`: passed for `v1.0.1`.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-v1.0.1-local.ps1 -WithScreenshots`: passed as the repeatable local acceptance command.
- `git diff --check`: no whitespace errors; only CRLF conversion warnings.
- Visual smoke with `ADMIN_SMOKE_SCREENSHOTS=1`: passed and generated 50 screenshots plus contact sheet; screenshot pixel smoke passed.

## Release And Deployment Checklist

1. Commit the current local changes.
2. Push to `chatops/main`.
3. Create or update tag `v1.0.1`.
4. Upload GitHub Release `UBot V1.0.1` with `release/ubot-1.0.1-win.zip`.
5. Deploy to production while preserving `.env`, `data/`, `config/groups.json`, and NapCat runtime state.
6. Verify production admin login, viewer login, read-only writes returning `readonly_session`, voice/default voice switch dependency, service health, and public backend route.

Before step 1, rerun:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-v1.0.1-local.ps1 -WithScreenshots
```
