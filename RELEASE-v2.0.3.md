# UBot V2.0.3 Release Notes

发布日期：2026-08-20

## 显式长文本

- 用户提出“3000 字”“三千字”“至少 N 字”“N 字以内”或明确要求长文时，本轮长度要求会覆盖默认短回复偏好。
- 模型在当前轮直接输出正文，不再只给预告、承诺或要求用户回复“发”“继续”。
- “发、继续、没写完、直接写完发出来”等追问可以继承同一因果链中当前用户最近一次未完成的长度目标，不读取其他用户或兄弟分支。
- 所有文字 Skill 均可使用；语音和唱歌模式保持原有长度策略。
- 采用单次模型调用。模型一次生成不足时发送其完整结果，不追加隐藏调用。

## Skill 预算与 QQ 分段

- 显式长文严格使用 Skill 的 `maxReplyCharsPerMessage`、`maxTotalReplyChars` 和 `maxReplyMessages`；普通回复继续使用 `preferredMaxReplyMessages`。
- 未填写回复控制的 Skill 在显式长文模式下默认使用每条 500 字、总计 3000 字、最多 8 条。
- 用户要求超过 Skill 上限时按 Skill 上限直接完成，不再次征求确认。
- 格式化器按自然段和标点优先拆分，单条、总字符和消息数上限与后台允许范围一致；总字符只统计实际正文，不再把气泡间虚拟空格算入预算。
- 主文字发送路径现已正确传递动态预算；多段真实 QQ 消息仍绑定同一个 assistant turn。

## 兼容性与验证

- 无数据库迁移，不清空短期上下文、长期记忆、日报或历史消息。
- `npm test`：526 项通过，并完成管理后台与服务端生产构建。
- 覆盖阿拉伯数字、中文数字、目标/最低/最高长度、跨轮继承、所有文字 Skill、3000 字无损分段和普通短回复回归。
