# UBot V3.0.7

发布日期：2026-08-28

## 本版更新

- 网页生成在主回复模型发生 408、429、5xx、网络错误或账号池不可用时，静默切换到严格绑定的 `ds` 回复模型；普通群聊的模型选择保持不变。
- 同一次生成请求在切换后保持使用备用模型，避免修复轮次重新请求不可用的主模型。所有模型均不可用时返回明确的稍后重试提示。
- 静态页面安全过滤器新增受限内联 SVG 几何、路径、分组和文字标签，并允许通过 CSS `@keyframes`、`transform` 与 `opacity` 制作 2D 动画。
- SVG 外链、`image`、`use`、`foreignObject`、SMIL、`xmlns`、`xlink:href`、`url()`、网络请求和事件属性继续失败关闭。
- 降级日志只记录模型 ID、安全化状态码、故障类别和结果，不记录密钥、提示词、页面内容或供应商正文。

## 兼容性

- 不新增数据库迁移，SQLite schema 仍为 migration `10`。
- 不改变预览 URL、管理 API、认证、生产端口或 30 天保留期限。
- 生产 `ds` 地址、模型和 API 密钥属于加密运行时设置，不包含在发布包中。

## 发布后检查

1. 验证主模型账号池不可用时，原始 SVG 鹈鹕动画需求会静默使用 `ds` 并发布一个预览链接。
2. 确认页面包含安全内联 SVG/CSS 动画、根链接返回 `200`，到期清理后返回 `404`。
3. 确认没有额外群提示、重复 Outbox、测试页面目录、密钥日志或数据库残留。
