# UBot V3.0.3

发布日期：2026-08-28

## 本版更新

- 后台侧边栏不再显示仅用于兼容跳转的空白菜单项。成员页面首次打开群组时会从 NapCat 拉取成员并写入短期缓存，保留手动刷新；连接失败会显示可操作的错误状态，而不是误报空成员列表。
- 会仙的日常表达调整为自然、连续的聊天风格，避免在普通交流中反复使用实现方式或身份标签。涉及照片、见面、位置、现实经历或外部操作等需要现实核验的话题，不凭空编造，并以简短自然的转场提供可继续的交流或可执行帮助。
- 新增持久化 Capability Policy 的启动校验与授权边界。聊天和 Provider 的视觉、流式、推理及请求超时能力只能被策略收窄，禁用时按拒绝优先处理；OpenAI-compatible 和官方 Anthropic Provider 继续受统一能力合约约束。
- 后台新增“账号与安全”页面。管理员可完成近期 MFA 重验、密码修改、TOTP 重置和恢复码轮换；超级管理员可管理邀请、账号状态、会话、群授权和认证审计。系统设置、会仙人格和全局指令等敏感写操作要求近期 MFA。
- 群参与判断和显式记忆请求解析已拆分为独立服务；维护作业通过 `MaintenanceScheduler` 集中定义间隔、顺序和防重入行为，维持既有七天留存约定。
- 迁移 importer 版本更新为 `3.0.3`。既有 V3 数据库采用增量升级路径，SQLite 迁移账本覆盖 `1` 至 `9`；本版会仙配置以带审计记录的单次修订写入现有权威数据库，不需手工修改 SQLite。

## 发布与兼容

- SQLite 仍是唯一业务状态源；已完成 cutover 的实例不会回退读取或双写旧 JSON。
- GitHub Actions 仅在匹配的最终 `v3.0.3` 标签上完成 Windows/Linux 测试、双平台打包、SHA-256 校验和正式 Release 创建。发布包不包含 `.env`、数据库、日志、群配置、私钥或持久人格资料。
- 生产部署只能使用匹配 GitHub Release 的 Linux 资产及其 SHA-256 清单。部署器会验证 Release 元数据、摘要、SQLite、Nginx、NapCat 和服务状态，失败时恢复 release 指针与服务配置，但不会自动回滚数据库或重放历史 Outbox。

## 部署后检查

1. 确认 `ubot-ingress.service`、`ubot-worker.service`、`ubot-admin.service` 均为 active，`ai-project.service` 保持 disabled/inactive。
2. 确认未登录的 `https://bot.9958.uk/api/health` 返回 `401`，并通过 HTTPS 完成管理员登录和需要的 TOTP 绑定。
3. 在现有群各执行一次 `@机器人` 与回复机器人消息的测试；成员页应自动加载成员，会仙应保持自然的上下文对话风格。
