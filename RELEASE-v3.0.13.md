# UBot V3.0.13

V3.0.13 将能力边界扩展到已允许 HTML 标签的被动属性，不包含数据库迁移、管理 API 或预览 URL 变更。

## 修复

- `type`、完整 ARIA、popover、布局提示等被动 HTML 属性默认允许。
- 网页和 SVG 不再因为模型采用新的纯展示属性名称而失败。
- `on*`、`src/srcdoc`、外部 `href`、提交地址、命名空间、URL/网络引用和脚本协议继续拒绝。

## 发布门槛

- 全量测试、双平台打包和 SHA-256 校验必须通过。
- 生产虚拟群以 GPT 主模型运行原始鹈鹕骑自行车请求，验证发布、HTTPS 200、过期 404、取消通知和无残留。
