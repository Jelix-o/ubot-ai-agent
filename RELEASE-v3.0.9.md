# UBot V3.0.9

V3.0.9 是 DeepSeek 实验模型静态网页输出协议的收尾补丁，不包含数据库迁移、管理 API 或预览 URL 变更。

## 修复

- DeepSeek 网页生成使用供应商 JSON 模式，避免复杂 HTML 中的转义错误。
- 完整 JSON 响应限制为 12,000 字符，避免在 8,192 token 上限处截断。
- 保留 V3.0.8 的关闭思考模式、定向修复请求和严格 SVG/CSS 安全边界。

## 发布门槛

- 全量测试、构建、Windows/Linux 打包和 SHA-256 校验必须通过。
- 使用原始“SVG 绘制鹈鹕骑自行车 2D 动画”请求完成虚拟群生产验收。
- 验收必须覆盖静默降级、HTTPS 200、过期 404、取消通知和无数据残留。
