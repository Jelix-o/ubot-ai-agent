# UBot V3.0.25

V3.0.25 将会仙的知识库从“提示词中预取少量 FAQ”升级为模型可调用、受边界约束的只读查询工具，解决追问、简称和非 FAQ 形式问法回退到模型猜测的问题。

## 功能

- 新增原生只读工具：`query_private_enterprise_ranking` 查询全局 2026 中国民营企业 500 强，`search_group_faq` 只查询当前群已启用的 FAQ。工具参数严格校验，模型不能传入或跨越群号。
- 榜单名次、省级家数、名单、比较、最多/最少、全国总数和城市上线门槛由校验后的确定性数据集直接返回，长名单保留总数和分批信息。未指定年份默认 2026；明确问其他年份不会套用本榜单。
- “阿里排几名”“第 2 名是谁”“哪个省最多”“湖北省和湖南省哪个多”及“分别是哪些”等追问均走确定性查询。唯一简称可查，歧义简称要求用户确认。
- FAQ 命中后只把当前群的已命中条目交给模型组织语言；无命中、参数无效或读取失败时直接说明无法核验，不让模型补造答案。原有群级 FAQ 编辑方式和后台只读榜单页不变。
- 支持 OpenAI 兼容协议及 Anthropic Messages 协议的工具调用/工具结果转换。对不支持原生工具的模型，事实类请求安全失败，不回退为未核验回答；普通聊天保持原有流式回复兼容路径。

## 验证与升级

- 覆盖 500 行完整性、简称和歧义、其他年份、省市口径、跨省比较、长名单分批、FAQ 群隔离、无结果安全失败、OpenAI/Anthropic 工具回合及后台只读权限。
- 已在生产当前选中的回复模型上验证原生工具调用和工具结果回合；发布包不修改线上 SQLite，部署仍通过现有备份、迁移和服务切换流程完成。
- 发布前运行 `node scripts/run-node22.cjs scripts/test.cjs`、`node scripts/run-node22.cjs scripts/visual-admin-smoke.mjs` 和 `npm run package:all`。
