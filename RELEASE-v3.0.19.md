# UBot V3.0.19

V3.0.19 新增可控的群聊文字生图能力，并支持多个 OpenAI 兼容图片上游的主备调度。

## 新功能

- 新增 `#画图 <提示词>` 和 `#生图 <提示词>`，每次生成并发送一张 `1024x1024` 图片。
- 后台模型配置新增“图片生成”分类，支持启停、选择主模型和调整备用模型顺序。
- 主模型遇到网络错误、超时、HTTP 408、429 或 5xx 时自动尝试备用模型；鉴权、审核、请求与响应格式错误不会切换。
- 每个群提供独立图片生成开关，所有已有群和新群默认关闭；每个“群 + QQ”成功生成后冷却 60 秒。

## 安全与运维

- 只接受合法 Base64 PNG、JPEG 或 WebP，限制 20 MB，不下载上游返回的 URL。
- 生成图片以权限受限的临时文件进入 outbox，Ingress 转换为 OneBot Base64，确认送达后删除；维护任务清理无 outbox 引用的孤儿文件。
- 图片模型密钥继续使用 V3 加密设置存储。单项连接检测明确提示费用，自动健康检查与批量检测跳过图片模型。
- V3 能力策略新增 `image_generation` 与 OpenAI `imageGeneration`，升级过程保持现有群开关关闭。

## 验证

- 覆盖多上游主备切换、非重试错误、取消与超时、Base64 和文件类型校验、路径越界、冷却、命令与 outbox 清理。
- 验证图片模型一次性配置的幂等性、密钥密文存储、现有对话模型保留和群开关默认关闭。
- 完整单元测试、后台视觉冒烟测试和双平台发布资产校验通过。
