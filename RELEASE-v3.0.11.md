# UBot V3.0.11

V3.0.11 扩充安全的 SVG 表现属性，不包含数据库迁移、管理 API 或预览 URL 变更。

## 修复

- 允许字体大小、字重、字体族、字距、填充透明度、描边透明度、填充规则和常用渲染提示。
- 允许经过格式校验的 `vector-effect`、`paint-order`、可见性等静态表现属性。
- 事件属性、外链、URL 引用、脚本协议、SMIL、`image`、`use` 和 `foreignObject` 继续拒绝。

## 发布门槛

- 全量测试、双平台打包和 SHA-256 校验必须通过。
- 生产虚拟群使用原始鹈鹕骑自行车请求，验证 GPT 输出可直接发布、HTTPS 200、过期 404、取消通知和无残留。
