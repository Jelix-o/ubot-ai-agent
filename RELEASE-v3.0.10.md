# UBot V3.0.10

V3.0.10 是 DeepSeek SVG 输出兼容性的最终收尾补丁，不包含数据库迁移、管理 API 或预览 URL 变更。

## 修复

- 在安全校验前仅移除标准、无行为的内联 SVG 默认命名空间声明。
- 发布后的页面仍不包含 `xmlns`；其他命名空间、`xlink:href`、外链、SMIL 和主动内容继续拒绝。
- 避免为删除标准命名空间而要求实验模型完整重画页面，消除修复轮输出膨胀和截断。

## 发布门槛

- 全量测试、构建、Windows/Linux 打包和 SHA-256 校验必须通过。
- 使用原始“SVG 绘制鹈鹕骑自行车 2D 动画”请求完成虚拟群生产验收。
- 验收必须覆盖静默降级、HTTPS 200、过期 404、取消通知、发布内容无命名空间及无数据残留。
