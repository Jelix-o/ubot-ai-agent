# UBot V3.0.8

V3.0.8 是 V3.0.7 网页生成降级链路的生产验收修正，不包含数据库迁移、管理 API 或预览 URL 变更。

## 修复

- DeepSeek 网页生成请求显式关闭思考模式，避免推理内容耗尽静态 HTML 的输出预算。
- 生成提示明确禁止 `meta`、`xmlns` 和未批准的 SVG 标签。
- 第二次安全修复请求携带安全化错误码，并再次声明 SVG/CSS 动画边界。
- 继续拒绝 SMIL、外链、网络请求、事件属性、`image`、`use` 和 `foreignObject`。

## 发布门槛

- 全量测试、构建、Windows/Linux 打包和 SHA-256 校验必须通过。
- 使用原始“SVG 绘制鹈鹕骑自行车 2D 动画”请求完成虚拟群生产验收，验证静默降级、HTTPS 200、过期 404、取消通知和无数据残留。
- 部署沿用原子发布与回滚机制；SQLite 迁移版本保持 1–10。
