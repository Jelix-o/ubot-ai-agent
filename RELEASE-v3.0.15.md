# UBot V3.0.15

V3.0.15 修复 V3 切换后所有 QQ 用户都无法执行群管理指令的问题。QQ 身份现在可以安全绑定到现有后台管理员账号，并实时继承该账号的权限。

## 修复

- 新增 SQLite v11 管理员 QQ 绑定表，QQ 与后台账号保持一对一关系。
- 后台安全页面支持在近期 TOTP 验证后绑定、更新或解绑 QQ，并写入认证审计。
- 超级管理员绑定的 QQ 可管理所有当前及未来群；群管理员绑定的 QQ 仅能管理已授权群。
- 账号停用、解绑或撤销群授权后，QQ 权限从下一条命令起立即失效。
- 群管理操作日志同时记录 QQ 发送者和对应后台账号身份。
- `#管理员`、密码、TOTP、恢复码和账号安全操作继续仅由网页后台管理。

## 发布门槛

- 全量测试、构建、双平台打包、差异检查和 SHA-256 校验必须通过。
- 生产升级后迁移版本必须为 1–11，并将 QQ `1569671790` 绑定到现有 `admin` 超级管理员账号。
- 三个 UBot 服务、Nginx、SQLite 完整性和监听端口必须正常，真实群 `#服务器` 必须通过鉴权。
