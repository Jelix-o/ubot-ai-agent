# UBot V3.0.26

V3.0.26 补齐了早期 V3 部署中可能遗漏的群知识库包，并将“2026 中国民营企业 500 强”从依赖模型选择工具，收紧为服务端优先的确定性问答。

## 修复

- 正式升级迁移会为每个已启用、但没有知识库包记录的群补建一个默认启用的空包；不读取或修改已退休的 JSON 配置，也不直接修改线上 SQLite。
- 后续通过后台启用或新建的群会在同一 SQLite 事务中获得缺失的默认知识库包，避免再次出现遗漏。
- 管理员曾主动关闭的知识库包保持关闭；修复只创建缺失记录，绝不重开已有的禁用包。
- 明确命名的榜单、唯一企业简称、前 N 名、名次区间、跨省比较和紧邻追问均直接查校验后的 500 行数据；附图不能再使榜单问题回退给模型。
- 明确的其他年份直接说明仅收录 2026；2025 年营收口径不会被误判为 2025 榜单。普通含“2026”的聊天、世界 500 强和电影榜单不会误用这份数据。
- 后台榜单搜索支持企业名称与精确名次（`2` 或 `第2名`）；群 FAQ 命中后直接返回管理员答案或确定性澄清，不再交给模型改写或拼接。

## 验证

- 覆盖已有启用群修复、新启用群自动建包、禁用包不被重开及已有 V3 切换升级的幂等性。
- 覆盖阿里简称、前 N/名次范围、其他年份、非目标榜单、普通年份聊天、长名单分批、附图榜单问答无模型回退、FAQ 命中终止式结果和后台精确名次筛选。
- 发布前运行全量测试、后台冒烟测试和双平台发布包校验；部署沿用备份、迁移和服务切换流程。
