# UBot V2.0.2 Release Notes

发布日期：2026-08-20

## 群聊日报修复

- 修复关闭日报 AI 文案后兜底中文乱码的问题；开启和关闭 AI 文案时均输出正常 UTF-8 中文。
- SQLite `messages` 表新增可空的 `sender_card`、`sender_nickname`，Ingress 和 Legacy 会保存消息发送时的群名片与 QQ 昵称快照，Worker 跨进程重建事件时不再丢失姓名。
- 日报名称优先级固定为：当前群名片 → 当前 QQ 昵称 → 消息中的群名片/昵称快照 → QQ 号。
- 定时日报与管理员 `#日报 发送/预览` 均在生成前读取一次当前群成员目录；成员接口失败时自动使用消息快照，不中断日报。
- 当天已经以 QQ 号记录的数据不做破坏性改写。日报生成时按 `userId` 实时补名，因此升级后下一次预览或发送即可显示当前群名片或昵称。

## 数据库与兼容性

- 旧数据库启动时自动补充两个可空字段，旧行继续正常读取。
- 本次升级不清空短期上下文、长期记忆、日报统计或历史审计消息，也不需要重新执行 V2.0.1 上下文迁移。
- 部署前备份 `data/shared/bot-shared.db` 与 `data/daily-report-store.json`；回滚到 V2.0.1 时新增列可以安全保留。

## 验证

- SQLite 迁移、写入与轮询覆盖群名片/昵称跨进程保存和旧行兼容。
- Ingress → Worker 集成测试覆盖发送者快照恢复。
- 日报测试覆盖群名片优先、昵称兜底、旧数字记录实时补名、成员查询失败快照兜底和 UTF-8 中文文案。
- Bot 测试覆盖定时日报和管理员预览均传入当前成员目录。
- `npm test`：518 项通过，并完成管理后台与服务端生产构建。
