# UBot V3.0.17

V3.0.17 深度重构 UBot 管理控制台交互体验与视觉设计系统，取消个人环境强制双因子认证，修复全系统成员下拉框空列表漏洞，并大幅提升复杂 2D SVG/HTML 网页动画生成与发布的容错能力。

## 改进与修复

- **认证极简化与操作时限解除（取消强制 2FA）**：
  - 默认关闭二次验证（MFA），密码验证通过后直接签发会话与 Cookie，单步一键登录后台。
  - 解除敏感操作必须在 10 分钟内完成二次验证（`requireRecentSuperAdminMfa`）的拦截，解决保存模型配置或生成邀请链接时频繁提示需要重新登录的问题。
- **全系统成员下拉框为空 Bug 修复**：
  - 修复 `memberProfileCache` 30 秒过短 TTL 导致的下拉成员列表清空问题，升级缓存时长至 15 分钟并引入 Stale-While-Revalidate（过期优先提供旧快照并在后台刷新）。
  - 群配置（GroupsView）与记忆管理（MemoriesView）在拉取成员时增加全量 `all=1` 支持与自动静默预加载。
- **复杂动画与 Gemini 网页生成增强**：
  - 重构 `parseStaticHtmlGeneration` 提取器，智能剥离 Markdown 代码块（```` ```json ```` / ```` ```html ````），支持提取外层包裹的 JSON 对象。
  - 增加原生 HTML 直接识别回退，模型无需强制格式化为纯文本 JSON 即可直接发布包含 `<title>` 的复杂 SVG 2D 动画。
  - 移除 12,000 字符硬性输出截断，保障高复杂度动画能够完整生成与解析。
- **全新视觉设计系统与 UI 交互升级**：
  - 引入毛玻璃（Glassmorphism）、微光动态状态指示（Pulsing Dot）、精细间距与 Typography 体系。
  - 群配置（GroupsView）升级为全新 iOS 风格 Toggle 滑块控件，按业务模块分为「回复与特性」、「定时与播报」、「成员与黑名单」三大 Tab。
  - 顶部导航条增加动态控制台面包屑与当前群号标签，边栏增加清晰的英文大写区块分类。
  - 极简单步登录面板（LoginView），兼顾颜值与效率。

## 验证

- 593 个单元测试全部通过。
- visual-admin-smoke 冒烟验证通过。
- 资产打包与双平台 SHA-256 校验通过。
