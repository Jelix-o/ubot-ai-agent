# UBot V3.0.18

V3.0.18 修复群聊图片理解链路，并发布表情包库与网页生成稳定性改进。

## 改进与修复

- **图片理解可靠性**：
  - 现有群统一启用图片理解，新接入群默认启用；管理员后续关闭某个群的图片理解仍会被保留。
  - 图片请求遇到当前文字模型不支持视觉时，仅该条消息临时使用已配置的 GPT 视觉模型，群内普通文字回复仍使用原模型。
  - 普通图片直接交给视觉模型回答，移除会因严格 JSON 格式而误拒绝图片的额外预检；代码截图预检失败时会降级为正常视觉回答。
  - 优化 QQ 图片本地化、超时和多图部分成功处理，支持 CQ 图片文本和回复消息中的图片输入。
- **表情包库**：
  - 超级管理员可维护持久化表情包、标签和关键词；普通对话在命中关键词、概率和群冷却条件后追加图片。
  - 黑名单成员有效 @ 机器人时继续只发送专用表情包，不调用模型。
- **网页生成**：
  - 网页预览使用独立输出预算和更长请求超时，检测输出截断后自动改用备用模型，避免发布不完整 HTML。

## 验证

- 完整单元测试、后台视觉冒烟测试和双平台发布资产校验通过。
- Linux 正式发布物部署到生产后，验证 Ingress、Worker、Admin、NapCat 反向连接和预览站点健康。
