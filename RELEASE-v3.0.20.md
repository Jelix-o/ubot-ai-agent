# UBot V3.0.20

V3.0.20 收紧图片生成权限，改善等待反馈，并修复 NapCat 回执超时导致同一图片被连续发送三次的问题。

## 权限与体验

- `#画图` 和 `#生图` 仅绑定后台 `super_admin` 账号的 QQ 可用；普通成员、群管理员、未绑定和已停用账号调用时完全静默。
- 图片生成不再提供群级开关，所有现有群和新群统一可用，系统级能力策略仍可全局停用。
- 提示词按 Unicode 字符计数，限制为 1–2000 字；超限提示会显示当前字数和需要删减的字数。
- 请求通过全部前置检查后先发送“正在生成图片，请稍候…”，成功后只发送图片。
- 普通成员的群内帮助不再展示图片生成命令，超级管理员帮助会显示权限、提示词上限和冷却规则。

## 投递可靠性

- NapCat 图片发送超时或连接在发送后断开时，按“投递结果不确定”终止该 outbox，不自动重发同一图片。
- 只有发送前明确未连接的图片任务可以重试；普通文本、语音和既有图片消息保持原重试行为。
- 成功投递仍保存真实 QQ 消息 ID 并删除临时文件；无法确认的图片由孤儿清理任务回收。

## 验证

- 覆盖 V3 超级管理员授权、群管理员和普通成员静默、帮助可见性、旧群开关兼容及 Unicode 长度边界。
- 覆盖生成前反馈、NapCat 投递结果分类、图片禁止歧义重试与普通 outbox 重试兼容。
- 完整单元测试、后台视觉冒烟测试和双平台发布资产校验通过。
