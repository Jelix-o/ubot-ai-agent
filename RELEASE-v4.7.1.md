# UBot v4.7.1

## 更新内容

- 系统设置的模型配置新增“检测全部模型”，可一次刷新所有模型健康状态。
- 系统状态和模型配置共享模型检测结果，检测不通过的模型分类页签会以红色背景提示。
- 后台所有单选框统一改为方框选中样式。
- 修复环境默认模型删除后被系统配置自动补回的问题。
- 模型检测失败现在返回完整模型状态，便于后台准确标记异常模型。

## 验证

- `npm run build`
- `npm test`
