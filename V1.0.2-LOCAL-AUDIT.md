# UBot V1.0.2 Local Completion Audit

Status: local implementation and verification complete. GitHub upload, tag push, GitHub Release upload, and production deployment are authorized for the V1.0.2 release flow.

## Requirement Evidence

| Requirement | Local evidence | Status |
| --- | --- | --- |
| System settings expose memory candidate threshold, long-term auto-approve threshold, and unattended mode. | `SystemSettings` backend/frontend types, `SystemSettingsStore`, `/api/system-settings`, and `SettingsView.vue` include the new fields. | Verified locally |
| Thresholds default to 60/80/false and legacy files backfill defaults. | `src/services/system-settings-store.test.ts` covers defaults, update normalization, invalid relations, and legacy settings files. | Verified locally |
| Invalid threshold ranges and candidate >= auto approve are rejected. | Store validation throws `invalid_memory_confidence_thresholds`; admin API maps it to HTTP 400; frontend validates before save. | Verified locally |
| Candidate extraction uses configurable thresholds. | `GroupMemoryCandidateService` reads `SystemSettingsStore`, skips below candidate threshold, pends middle-confidence candidates, and auto-approves high-confidence candidates. | Verified locally |
| Unattended mode does not bypass safety protections. | Tests cover candidate-threshold auto approval plus pending behavior for non-Chinese normalization failure and unclear member profile attribution. | Verified locally |
| AI extraction prompt matches configured thresholds. | `src/services/ai-service.test.ts` verifies dynamic threshold percentages are included in the prompt. | Verified locally |
| Below-threshold English candidates do not enter language-review pending queue. | `below-threshold English candidates do not enter language review pending queue` covers filtering before language normalization. | Verified locally |
| Memory deduplicate preview supports fast/deep modes and progress. | `MemoriesView.vue`, `AdminTaskStore`, `AdminHttpServer`, and dedup tests cover task progress and semantic-mode behavior. | Verified locally |
| Group config supports roast mode users. | `GroupBotConfig`, `GroupConfigService`, admin group UI, bot active-chat tracking, and bot tests cover `roastModeUserIds`. | Verified locally |
| MiMo TTS uses clean assistant text. | `src/utils/tts-text.test.ts` and `src/services/tts-service.test.ts` verify style instructions stay out of assistant target text and singing only keeps `(唱歌)`. | Verified locally |
| Release metadata is aligned to V1.0.2. | `package.json`, `README.md`, `COMMANDS.md`, `RELEASE-v1.0.2.md`, packaging, GitHub Release script, workflow, and local verifier are checked by static tests. | Verified locally |
| Sensitive runtime config is not packaged. | Zip checks show no real `.env`, `config/groups.json`, `system-settings.json`, or runtime logs are included. | Verified locally |

## Latest Local Verification

- `npm run build:admin`: passed.
- `npm run build:server`: passed.
- `npm test`: 369/369 passed.
- `node scripts/run-node22.cjs node_modules/tsx/dist/cli.mjs --test src/services/group-memory-candidate-service.test.ts`: 13/13 passed.
- `git diff --check`: no whitespace errors; only CRLF conversion warnings.
- `npm run package:win`: passed and generated `release/ubot-1.0.2-win.zip`.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/publish-github-release.ps1 -DryRun`: passed for `v1.0.2`.

## Release And Deployment Checklist

1. Commit the current local changes.
2. Push to `main`.
3. Create and push tag `v1.0.2`.
4. Upload GitHub Release `UBot V1.0.2` with `release/ubot-1.0.2-win.zip`.
5. Deploy to production while preserving `.env`, `data/`, `config/groups.json`, and NapCat runtime state.
6. Verify production service health, NapCat reconnect, admin API auth, system-settings fields, and threshold validation.

Before upload or deploy, rerun:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-v1.0.2-local.ps1
```
