# UBot V3.0.23

V3.0.23 在保留图片生成、上下文与身份识别修复的基础上，完成语音功能退役、后台密码登录收敛和后台界面更新。

## 改进与修复

- 退役 TTS、语音和唱歌运行链路及其配置入口；历史语音配置会在迁移中移除，旧写入请求返回 `voice_feature_retired`。
- 后台登录改为账号密码与邀请码注册，敏感操作仍需近期密码复验；新增 `npm run admin:reset-password` 服务器端交互式改密工具。
- QQ 群主和群管理员自动拥有本群管理指令权限，但不会因此取得后台登录或跨群权限。
- 保留 `#画图` / `#生图` 的超级管理员限制、5000 个 Unicode 字符提示词上限、生成进度反馈、60 秒冷却，以及 `generated_image` 不确定投递不重试策略。
- 保留最近群聊上下文、别名识别和图片模型主备配置；图片模型仍只允许 OpenAI 兼容协议。
- 更新后台视觉样式与密码登录、模型配置页面的可用性。

## 升级注意

- 部署会执行数据库备份、完整性检查和迁移，并撤销现有后台会话。
- 语音模型密钥、语音能力设置和语音角色配置会被清理；图片模型配置不受影响。
- 部署前必须确认可重试 outbox 已排空，避免旧版语音 outbox 被错误投递。

## 验证

- `node scripts/run-node22.cjs scripts/test.cjs`
- `node scripts/run-node22.cjs scripts/visual-admin-smoke.mjs`
- `npm run package:all`
