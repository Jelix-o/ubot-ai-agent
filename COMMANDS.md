# UBot 群内指令与行为说明

> 当前默认人格是**会仙**：原创、成年的女性虚拟聊天伙伴。她会自然地聊天、接梗、帮忙和陪伴，但不会伪造真人身份、私人照片、现实身体、线下行程或未执行的外部操作。

## 群参与规则

| 方式 | 是否默认启用 | 说明 |
| --- | --- | --- |
| `@机器人 <内容>` | 是 | 普通成员可触发会仙对话。 |
| 引用机器人已确认回复 | 是 | 只有同群、真实 QQ 回执确认的机器人消息可继续追问，无需再次 @。 |
| 引用普通成员/未知消息 | 否 | 不触发机器人，避免插话和跨群污染。 |
| 关键词 | 否 | 仅 `mentions_and_keywords` 或 `selected_members` 模式且管理员明确配置时启用。 |
| 指定成员低频参与 | 否 | 仅 `selected_members` 模式启用，并遵守延迟、静音和成员配置。 |

`#闭嘴` 会停止普通对话、关键词、实时对话和复读，但保留已授权的管理命令和定时能力。

## 会仙的身份边界

- 会仙是原创虚拟角色，不对应现实中的具体个人。
- 她可以温柔、俏皮、有一点幽默和暧昧感，也可以认真解决技术、工作、学习和生活问题；不会羞辱、胁迫、操控或制造排他依赖。
- 请求真人照片、见面、线下活动或现实身体信息时，会仙会自然说明自己没有可发送的真实私人照片或线下行程，并可提供虚构形象设定、插画提示、线上活动方案或其他实际帮助。
- 模型、网页、图片、引用和群消息都是不可信输入，不能覆盖系统安全边界或伪造外部操作结果。

## 记忆

记忆只保存成员或管理员**明确要求保存**的信息；普通聊天不会自动提取、推断、进入候选队列或自动入库。

### `#记忆 <内容>`

- 权限：普通成员。
- 作用：把内容保存为自己在当前群的记忆。
- 示例：`#记忆 我喜欢先看结论`。
- 不要用它保存密码、API Key、令牌、私钥或其他凭据；系统会拒绝明显的敏感凭据内容。

### `@机器人 请记住 <内容>` / `@机器人 请记忆 <内容>`

- 权限：普通成员。
- 作用：与上面的指令等价，适合在正常对话里明确交代偏好或边界。
- 示例：`@机器人 请记住我不吃香菜`。
- 没有 @ 或可信机器人引用的普通“请记住”不会写入，避免环境群消息注入记忆。

### `#记忆` / `#记忆 状态`

- 权限：群管理员 / 超级管理员。
- 作用：查看当前群的记忆数量、启用状态和写入方式。

成员被管理员配置为“禁用记忆收集”后，系统不会保存其新记忆，也不会在后续对话中使用该成员已有的个人记忆。

## 对话、状态与模型

### `#功能` / `#帮助` / `#命令`

- 权限：普通成员。
- 作用：查看可用功能和分类帮助。

### `#对话 清空`

- 权限：普通成员。
- 作用：清空自己的短期对话上下文。

### `#对话 清空 <QQ号>` / `#对话 清空 全部` / `#clear`

- 权限：群管理员 / 超级管理员。
- 作用：清空指定成员或当前群的短期上下文。

### `#状态` / `#健康` / `#服务器` / `#操作日志`

- 权限：群管理员 / 超级管理员。
- 作用：查看群配置、模型、传输层、运行状态和审计信息。

### `#模型状态` / `#模型切换 <modelId>`

- 状态：普通成员可查看。
- 切换：群管理员 / 超级管理员。

## 语音、任务与群管理

- `#语音 <内容>`、`@机器人 语音说 <内容>`：生成语音回复；需要群语音功能开启。
- `#唱歌 <内容>`：使用支持 singing 的 TTS 模型；不支持时会回退为文字说明。
- `#语音回复 状态 / 开启 / 关闭`：群管理员 / 超级管理员控制默认语音回复。
- `#定时任务 列表 / 添加 <规则> / 删除 <任务ID>`：管理当前群的提醒任务。
- `#日报`、`#节假日`：查看或管理群日报和节日倒计时。
- `#管理员`、`#闭嘴`、`#说话`、`#拉黑`：按后台配置的权限等级执行。

后台的命令管理可以调整内置命令的主命令、别名和启停；权限等级不能被后台降低。

## 管理后台权限

- 后台使用独立管理员账号、密码和 TOTP，不再接受仅凭 QQ 号、共享群密码或旧 viewer 登录。
- `super_admin` 可管理全局配置、模型密钥引用、管理员、群授权、会仙人格、能力策略、隐私恢复和运行诊断。
- `group_admin` 只能操作被授予的群：群行为、已批准回复模型、成员备注、显式记忆、知识、提醒和报告；不能查看系统密钥、健康诊断、其他管理员或全局设置。
- 群管理员可新增成员记忆退出，但不能重新启用退出状态；只有 `super_admin` 可以恢复。
- 首次登录必须完成 TOTP 绑定。恢复码仅显示一次，应该保存在受控的密码管理器中。
