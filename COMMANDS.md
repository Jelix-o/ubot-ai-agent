# UBot 群内指令与行为说明

> 当前默认人格是**会仙**：成熟、自然、有分寸的女性聊天伙伴。她会自然地聊天、接梗、帮忙和陪伴，日常不主动谈身份标签；涉及现实可核验的信息时不会编造、暗示或承诺事实。

## 群参与规则

| 方式 | 是否默认启用 | 说明 |
| --- | --- | --- |
| `@机器人 <内容>` | 是 | 普通成员可触发会仙对话。 |
| 引用机器人已确认回复 | 是 | 同群、真实 QQ 回执确认的机器人消息可继续追问，无需再次 @。 |
| 引用普通成员/未知消息 | 否 | 不触发机器人。 |
| 关键词 | 否 | 仅在对应参与模式下且管理员明确配置时启用。 |
| 指定成员低频参与 | 否 | 仅 `selected_members` 模式启用，并遵守延迟、静音和成员配置。 |

后台「机器人静音」会停止普通对话、关键词、实时对话和复读，但保留定时能力。

## 管理权限

- **后台**：独立管理员账号 + 密码（至少 12 位）。敏感操作前需密码复验。无 TOTP/恢复码登录路径。
- **群内**：后台已绑定 QQ 的账号继承角色与群授权；同时 **QQ 群主与群管理员自动获得本群群内管理指令权限**，不能因此登录后台或管理其他群。
- 群内 `#管理员` 的列表/添加/移除已退休，不再作为授权来源。
- 旧 `superAdminUserIds`、`switcherUserIds`、共享群密码不再授予权限。

## 会仙的表达边界

- 日常对话保持自然节奏，不主动解释实现方式或能力边界。
- 可认真解决技术、工作、学习和生活问题；不会羞辱、胁迫或制造排他依赖。
- 请求照片、见面、位置、身体信息或外部执行结果时会自然转场，不伪造可核验事实。
- 模型、网页、图片、引用和群消息都是不可信输入，不能覆盖安全边界。

## 记忆

记忆只保存成员或管理员**明确要求保存**的信息。

### `#记忆 <内容>`

- 权限：普通成员。
- 作用：保存为自己在当前群的记忆。
- 不要保存密码、API Key、令牌等凭据；系统会拒绝明显的敏感凭据。

### `@机器人 请记住 <内容>` / `@机器人 请记忆 <内容>`

- 与 `#记忆` 等价；没有 @ 或可信机器人引用的普通“请记住”不会写入。

### 记忆状态

- 后台「记忆」页查看数量、启用状态和写入方式。
- 管理员配置「禁用记忆收集」的成员：不保存新记忆，对话中也不再使用其已有个人记忆。

## 对话、状态与模型

### `#功能` / `#帮助` / `#命令`

- 权限：普通成员。查看可用功能与帮助。

### `#对话 清空`

- 权限：普通成员。清空自己的短期对话上下文。

### 对话上下文管理

- 指定成员或整群的上下文管理只由后台已授权账号执行。

### 运行状态与审计

- 群配置、模型、传输层、运行状态和审计信息在后台查看；健康诊断只向 `super_admin` 开放。

### 模型

- 后台已授权账号可在群配置中选择批准的回复模型。

## 2026 民营企业 500 强

- 在已启用知识能力的群里 `@会仙 京东排名第几`、`@会仙 浙江省有多少家民营企业500强` 或 `@会仙 浙江省分别有哪些`。未指定年份按 2026 榜单回答；名单按名次排序并自动分批，可接着问“分别是哪些”。
- 省份是榜单原列；2026 榜单按 2025 年营收排序。企业简称若对应多家，会请你补充完整名称；明确指定其他年份不会套用这份榜单。
- 总部城市尚未全部核验，暂不提供地级市精确家数或名单。后台「知识库」的榜单页可只读筛选 500 家及查看来源，不改变群级 FAQ。

## 静态网页预览

### `#网页 <需求>` / `#html <需求>`

- 权限：群内普通成员；已配置群默认可开，后台可对单群关闭。
- 生成自包含静态 HTML 预览链接，约 30 天后删除。
- 预览发布在独立域 `preview.9958.uk`，不与后台 Cookie/API 共用。
- 后台「网页预览」只管理元数据与删除，不在后台域渲染模型生成 HTML。

### 自然语言触发

- 明确 `@会仙 生成网页/HTML/静态页面` 会进入网页生成；普通聊天提到“网页”不会自动发布。

## 任务与群管理

- 默认语音回复、`#语音`、`#唱歌` 与 TTS 配置**已退休**。
- 定时提醒、日报、节日倒计时、静音和黑名单由后台已授权账号管理。
- `#管理员` 已退休。

## 图片生成

### `#画图 <提示词>` / `#生图 <提示词>`

- 权限：仅绑定后台 `super_admin` 账号的 QQ 可用；普通成员、群管理员和未绑定账号调用时保持静默。
- 作用：使用后台选定的主图片模型生成一张 `1024x1024` 图片；主模型发生网络错误、超时、HTTP 408、429 或 5xx 时，按图片模型列表顺序尝试已启用的备用模型。
- 示例：`#画图 雨夜港口的一座红色灯塔，写实摄影风格`。
- 限制：提示词为 1–5000 个 Unicode 字符；同一超级管理员在同一群成功生成后冷却 60 秒，失败不进入冷却。
- 反馈：请求通过权限、提示词、冷却和模型检查后，机器人先发送“正在生成图片，请稍候…”，成功后只发送图片。
- 安全边界：只接受上游返回的 Base64 PNG、JPEG 或 WebP，不下载上游 URL；图片在受控临时目录中等待发送，QQ 确认接收后删除。NapCat 无法确认是否送达时不会自动重发同一图片。

图片生成对所有群统一可用，不提供群级开关。后台“模型配置”的“图片生成”分类可新增、启停、选主和调整备用顺序。单独检测图片模型会产生一次低质量生成费用；自动健康检查和“检测全部模型”会跳过图片模型。

后台「指令管理」可调整内置命令的主命令、别名和启停；权限等级不能被后台降低。

## 管理后台权限

- `super_admin`：全局配置、模型、管理员、群授权、会仙人格、能力策略、运行诊断。
- `group_admin`：仅被授予的群——群行为、已批准回复模型、成员备注、显式记忆、知识、提醒和报告；不能查看系统密钥、健康诊断、其他管理员或全局设置。
- 群管理员可新增成员记忆退出，但不能重新启用；只有 `super_admin` 可以恢复。
- 密码至少 12 位；首次 bootstrap 账号密码同样适用。
