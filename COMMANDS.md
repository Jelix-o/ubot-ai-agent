# UBot V3.0.0-rc.1 群内指令与行为说明

> RC.1 采用保守参与策略：机器人默认只在被 @ 或被引用已确认回复时参与。后台中的群管理员网页登录尚未启用，不能用 QQ 号和共享密码替代真实身份认证。

后台“指令管理”可配置内置指令的主命令、别名和启停；权限等级本身不可由后台降低。

## 权限

- **普通成员**：当前群内任意成员。
- **群管理员**：当前群 `switcherUserIds` 中的 QQ，或超级管理员。
- **超级管理员**：`config/groups.json` 顶层 `superAdminUserIds` 中的 QQ。

群管理员可以使用当前群的运营命令；超级管理员可管理所有群和群管理员。管理后台当前只接受受保护的超级管理员登录，公共群管理员登录会被拒绝，直到接入可验证身份方案。

## 群参与规则

| 方式 | 是否默认启用 | 说明 |
| --- | --- | --- |
| `@机器人 <内容>` | 是 | 普通成员可触发对话。 |
| 引用机器人已确认回复 | 是 | 同群内引用真实 QQ 回执后的机器人消息可继续同一因果链，无需再次 @。 |
| 引用普通成员/未知消息 | 否 | 不触发机器人，防止插话和跨群污染。 |
| 关键词 | 否 | 仅 `mentions_and_keywords` 或 `selected_members` 群模式下，且后台显式配置关键词时启用。 |
| 指定成员低频参与/复读 | 否 | 仅 `selected_members` 群模式下启用，并遵守延迟、静音和成员配置。 |

`#闭嘴` 会停止普通对话、关键词、实时对话和复读，但保留管理命令、日报、节假日和定时任务等显式能力。

## 帮助与状态

### `#功能` / `#帮助` / `#命令`

- 权限：普通成员
- 作用：查看系统功能总览。

### `#功能 <分类>` / `#帮助 <分类>` / `#命令 <分类>`

- 权限：普通成员
- 分类：`对话`、`语音`、`技能`、`实时对话`、`定时任务`、`日报`、`节假日`、`管理员`、`权限`。

### `#状态`

- 权限：群管理员 / 超级管理员
- 作用：查看当前群的说话状态、Skill、回复模型、参与配置、语音、定时任务、日报、节假日、黑名单和管理员信息。

### `#健康检查` / `#健康`

- 权限：群管理员 / 超级管理员
- 作用：查看 NapCat、Skill、模型和群功能配置状态。

### `#服务器`

- 权限：群管理员 / 超级管理员
- 作用：查看主机、Node、进程、运行时长、资源和 NapCat 状态。

### `#操作日志`

- 权限：群管理员 / 超级管理员
- 作用：查看当前群最近的管理操作日志。

### `#告警 状态` / `#告警 开启` / `#告警 关闭`

- 权限：群管理员 / 超级管理员
- 作用：查看或切换当前群自动运维告警。

## 对话与上下文

### `@机器人 <内容>`

- 权限：普通成员
- 作用：使用当前 Skill 生成文字或默认语音回复。
- 上下文：按“群 + 因果分支”持久化。明确引用只在同群、真实机器人回执、1 小时窗口内恢复精确链路；无引用仅允许同一用户在 10 分钟内的短追问或高相似度续聊。

### `#对话 清空`

- 权限：普通成员
- 作用：清空自己在当前群的短期上下文。

### `#对话 清空 <QQ号>` / `#对话 清空 @QQ`

- 权限：群管理员 / 超级管理员
- 作用：清空指定成员在当前群的短期上下文。

### `#对话 清空 全部` / `#clear`

- 权限：群管理员 / 超级管理员
- 作用：清空当前群所有短期上下文与未过期话题。

## 语音

### `#语音 <内容>` / `@机器人 语音说 <内容>`

- 权限：普通成员
- 作用：生成 AI 回复后合成为语音条。
- 条件：当前群 `voiceReplyEnabled=true`，且语音命令在后台启用。

### `#语音回复 状态` / `#语音回复 开启` / `#语音回复 关闭`

- 权限：群管理员 / 超级管理员
- 作用：查看或控制普通 AI 回复是否默认发送语音。

### `#唱歌 <内容>`

- 权限：普通成员
- 作用：以 singing 模式生成并发送语音。
- 条件：需要支持 singing 的 TTS 模型；不支持时会说明并回退文字。

## Skill 与模型

### `#技能 列表`

- 权限：普通成员
- 作用：查看当前群允许的 Skill。

### `#技能 切换 <skillId>`

- 权限：群管理员 / 超级管理员
- 作用：切换当前群 Skill，并清空该群短期上下文。

### `#模型状态` / `#模型 列表`

- 权限：普通成员
- 作用：查看当前群回复模型和可切换模型。

### `#模型切换 <modelId>`

- 权限：群管理员 / 超级管理员
- 作用：切换当前群回复模型。

## 实时对话

这些命令仅在群参与方式为 `selected_members` 时会产生低频主动参与效果。

### `#实时对话 列表`

- 权限：群管理员 / 超级管理员
- 作用：查看实时对话 QQ 列表和倒计时。

### `#实时对话 添加 <QQ号>` / `#实时对话 移除 <QQ号>`

- 权限：群管理员 / 超级管理员
- 作用：管理低频参与跟踪名单。

### `#实时对话 间隔 <分钟>`

- 权限：群管理员 / 超级管理员
- 作用：设置机器人发言后的安静时间。

## 记忆、知识与画像

### `#记忆 状态` / `#知识库 状态`

- 权限：群管理员 / 超级管理员
- 作用：查看当前群记忆、候选和知识库状态。

### `#昨日画像 <备注/QQ号>` / `#群聊画像 <备注/QQ号>`

- 权限：普通成员
- 作用：查询指定成员的昨日或整体画像；省略参数时查询自己。
- 边界：画像和证据仅在认证后台管理，不生成公共分享链接。

## 定时任务、日报与节假日

### `@机器人 设置定时任务一个小时提醒群友喝水`

- 权限：普通成员
- 作用：按自然语言创建循环提醒任务。

### `#定时任务 列表` / `#定时任务 添加 <规则>` / `#定时任务 删除 <任务ID>`

- 权限：普通成员
- 作用：查看、添加、删除当前群定时任务。

### `#定时任务 状态` / `#定时任务 开启` / `#定时任务 关闭`

- 状态：普通成员可查看。
- 开关：群管理员 / 超级管理员可控制当前群全部任务。

### `#日报` / `#日报 状态` / `#日报 发送` / `#日报 开启` / `#日报 关闭` / `#日报 时间 <HH:mm>`

- 状态：普通成员可查看。
- 发送、开关、时间：群管理员 / 超级管理员。

### `#节假日` / `#节假日 状态` / `#节假日 发送` / `#节假日 开启` / `#节假日 关闭` / `#节假日 时间 <HH:mm>`

- 状态：普通成员可查看。
- 发送、开关、时间：群管理员 / 超级管理员。

## 管理员、静音与黑名单

### `#管理员` / `#管理员 列表`

- 权限：群管理员 / 超级管理员
- 作用：查看当前群管理员和超级管理员。

### `#管理员 添加 <QQ号>` / `#管理员 移除 <QQ号>`

- 权限：超级管理员
- 作用：管理当前群管理员。

### `#闭嘴` / `#说话`

- 权限：群管理员 / 超级管理员
- 作用：暂停或恢复当前群普通说话能力。

### `#拉黑 <QQ号>` / `#拉黑 解除 <QQ号>`

- 权限：群管理员 / 超级管理员
- 作用：管理不应收到机器人回复的成员；被拉黑成员的消息可仍保留在日报或审计路径中。

## 输出与安全规则

- AI 正文中的第三方 `@QQ`、`@名字` 或 CQ at 会被降级成普通文本，避免误触发第三方提醒。
- 模型、网页、图片、引用和群消息均是不可信输入，不能覆盖系统安全边界或伪造外部操作结果。
- 机器人不得假装已经执行外部操作、见过线下事件或拥有真实人类身份。
- 管理员可以通过后台审计与参与决策记录理解机器人为什么回复或保持观察。
