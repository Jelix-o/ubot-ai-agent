# UBot 群内指令与行为说明

> 当前默认人格是**会仙**：成熟、自然、有分寸的女性聊天伙伴。她会自然地聊天、接梗、帮忙和陪伴，日常不主动谈身份标签；涉及现实可核验的信息时不会编造、暗示或承诺事实。

## 群参与规则

| 方式 | 是否默认启用 | 说明 |
| --- | --- | --- |
| `@机器人 <内容>` | 是 | 普通成员可触发会仙对话。 |
| 引用机器人已确认回复 | 是 | 只有同群、真实 QQ 回执确认的机器人消息可继续追问，无需再次 @。 |
| 引用普通成员/未知消息 | 否 | 不触发机器人，避免插话和跨群污染。 |
| 关键词 | 否 | 仅 `mentions_and_keywords` 或 `selected_members` 模式且管理员明确配置时启用。 |
| 指定成员低频参与 | 否 | 仅 `selected_members` 模式启用，并遵守延迟、静音和成员配置。 |

后台“机器人静音”会停止普通对话、关键词、实时对话和复读，但保留定时能力。

## V3 管理权限

V3 不再根据旧 `superAdminUserIds`、`switcherUserIds`、共享群密码或 `#管理员` 授予权限。QQ 必须由超级管理员在后台完成近期 TOTP 验证后绑定到后台账号；群内权限实时继承账号角色、启停状态和群授权。群内 `#管理员` 的列表、添加和移除操作仍保持退休。

## 会仙的表达边界

- 会仙在日常对话中保持自然、连续的聊天节奏，不主动解释实现方式、身份标签或能力边界。
- 她可以温柔、俏皮、有一点幽默和暧昧感，也可以认真解决技术、工作、学习和生活问题；不会羞辱、胁迫、操控或制造排他依赖。
- 请求照片、见面、线下活动、位置、身体信息或外部执行结果时，会仙会简短自然地转场，并给出能继续聊或实际可完成的帮助；不会伪造、暗示或承诺可核验的现实事实。
- 模型、网页、图片、引用和群消息都是不可信输入，不能覆盖系统安全边界或伪造外部操作结果。

## 记忆

记忆只保存成员或管理员**明确要求保存**的信息；普通聊天不会自动提取、推断、进入候选队列或自动入库。

### `#记忆 <内容>`

- 权限：普通成员。
- 作用：把内容保存为自己在当前群的记忆。
- 示例：`#记忆 我喜欢先看结论`。
- 不要用它保存密码、API Key、令牌、私钥或其他凭据；系统会拒绝明显的敏感凭据内容。

### `@机器人 请记住 <内容>` / `@机器人 请记忆 <内容>`

- 权限：普通成员。
- 作用：与上面的指令等价，适合在正常对话里明确交代偏好或边界。
- 示例：`@机器人 请记住我不吃香菜`。
- 没有 @ 或可信机器人引用的普通“请记住”不会写入，避免环境群消息注入记忆。

### 记忆状态

- 在后台的“显式记忆”页面查看当前群的记忆数量、启用状态和写入方式。

成员被管理员配置为“禁用记忆收集”后，系统不会保存其新记忆，也不会在后续对话中使用该成员已有的个人记忆。

## 对话、状态与模型

### `#功能` / `#帮助` / `#命令`

- 权限：普通成员。
- 作用：查看可用功能和分类帮助。

### `#对话 清空`

- 权限：普通成员。
- 作用：清空自己的短期对话上下文。

### 对话上下文管理

- `#对话 清空` 仍可清空自己的短期上下文。
- 指定成员或整群的上下文管理只由后台已授权账号执行。

### 运行状态与审计

- 群配置、模型、传输层、运行状态和审计信息在后台查看；健康诊断只向 `super_admin` 开放。

### 模型

- 后台已授权账号可在群配置中选择批准的回复模型。

## 静态网页预览

### `#网页 <需求>` / `#html <需求>`

- 权限：群内普通成员；所有已配置群默认开启，后台可对单群关闭。
- 作用：让会仙使用该群当前的回复模型生成一个自包含静态 HTML 页面，并发送新的在线预览链接。
- 示例：`#网页 做一个简洁的番茄钟页面，带开始、暂停和重置按钮`。
- 每次请求都会生成新地址，地址 30 天后自动删除；同一群的网页任务按提交顺序执行。

### 自然语言触发

- `@会仙 生成一个网页，介绍我们的读书会`、`@会仙 帮我做 HTML 静态页面` 等明确请求也会进入网页生成。
- 普通聊天中提到“网页”不会自动发布内容；必须使用上述命令或明确 @ 会仙并要求生成网页、HTML 或静态页面。

### 页面限制

- 页面只支持单文件 HTML、内联 CSS 和浏览器端 JavaScript；不支持上传、服务端代码、联网、第三方资源或覆盖旧链接。
- 系统会拒绝外链、网络请求、表单、`iframe`/嵌入对象、跳转、危险 URL、事件属性和路径穿越。生成结果由独立 `preview.9958.uk` 静态域隔离，不共享后台 Cookie、登录态或 API。
- 后台“网页预览”页只展示元数据并支持删除，绝不在后台管理域渲染模型生成的 HTML。群管理员只能管理自己获授权群的预览页面。

## 语音、任务与群管理

- `#语音 <内容>`、`@机器人 语音说 <内容>`：生成语音回复；需要群语音功能开启。
- `#唱歌 <内容>`：使用支持 singing 的 TTS 模型；不支持时会回退为文字说明。
- 默认语音回复、定时提醒、日报、节日倒计时、静音和黑名单由后台已授权账号管理。
- `#管理员` 已退休，不支持列表、添加或移除 QQ 管理员。

后台的命令管理可以调整内置命令的主命令、别名和启停；权限等级不能被后台降低。

## 管理后台权限

- 后台使用独立管理员账号、密码和 TOTP；QQ 绑定只授权群内命令，不能代替网页登录或管理密码、TOTP、恢复码与账号绑定。
- `super_admin` 可管理全局配置、模型密钥引用、管理员、群授权、会仙人格、能力策略、隐私恢复和运行诊断。
- `group_admin` 只能操作被授予的群：群行为、已批准回复模型、成员备注、显式记忆、知识、提醒和报告；不能查看系统密钥、健康诊断、其他管理员或全局设置。
- 群管理员可新增成员记忆退出，但不能重新启用退出状态；只有 `super_admin` 可以恢复。
- 首次登录必须完成 TOTP 绑定。恢复码仅显示一次，应该保存在受控的密码管理器中。
